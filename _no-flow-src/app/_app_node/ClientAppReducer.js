//

// import { UPDATE_PROFILE } from '../actions/profile';
// import ProfileProvider from '../nodeComponents/ProfileProvider';

// const profileProvider = new ProfileProvider('config/profiles.json')

export default async function clientAppReducer(action) {
  // console.log(`(${new Date().toTimeString()} [ClientAppReducer]: IPC recieved`, args);
  // switch (action.type) {
  //   case UPDATE_PROFILE:
  //     return [UPDATE_PROFILE, profileProvider.getProfiles()];
  // }
  // return [UPDATE_PROFILE, 'cake'];
}
